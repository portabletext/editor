# Pilcrow

Skeleton app. Real work happens on the long-running feature branch and lands on this skeleton over time via Vercel preview deploys.

## Develop

```
pnpm --filter pilcrow dev
```

## Build

```
pnpm --filter pilcrow build
```

The build output goes to `apps/pilcrow/dist/` and is suitable for static hosting.
