/**
 * `@portabletext/markdown` v2 spike — internal entry points.
 *
 * Not exported from the package's public surface. Reachable via
 * `@portabletext/markdown/src/v2` for the spike's measurement tests.
 *
 * @internal
 */

export {markdownToPortableTextV2} from './markdown-to-portable-text'
export {portableTextToMarkdownV2} from './portable-text-to-markdown'
